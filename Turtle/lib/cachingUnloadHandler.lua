--region *.lua

os.loadAPI("/terp/lib/terp")
os.loadAPI("/terp/lib/waypointCollection")

-- Create an unload handler that deploys a chest in place
-- Might be used in conjunction with a depot handlers for rolling unload
-- points.
function create(slot)
  local self = { }

  

end


--endregion
